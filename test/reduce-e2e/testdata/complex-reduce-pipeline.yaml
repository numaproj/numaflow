apiVersion: numaflow.numaproj.io/v1alpha1
kind: Pipeline
metadata:
  name: complex-sum
spec:
  limits:
    readBatchSize: 1
  vertices:
    - name: in
      scale:
        min: 1
      source:
        http: {}
    - name: atoi
      scale:
        min: 1
      udf:
        container:
          # Tell the input number is even or odd, see https://github.com/numaproj/numaflow-go/tree/main/pkg/function/examples/evenodd
          image: quay.io/numaio/go-even-odd-example
    - name: first-aggregation
      udf:
        container:
          # compute the sum
          image: quay.io/numaio/go-integer-sum-example
        groupBy:
          window:
            fixed:
              length: 10s
          keyed: true
    - name: second-aggregation
      udf:
        container:
          # compute the sum
          image: quay.io/numaio/go-integer-sum-example
        groupBy:
          window:
            fixed:
              length: 60s
          keyed: false
    - name: sink
      scale:
        min: 1
      sink:
        log: {}
  edges:
    - from: in
      to: atoi
    - from: atoi
      to: first-aggregation
      parallelism: 2
    - from: first-aggregation
      to: second-aggregation
    - from: second-aggregation
      to: sink
