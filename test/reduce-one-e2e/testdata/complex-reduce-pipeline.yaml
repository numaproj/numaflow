apiVersion: numaflow.numaproj.io/v1alpha1
kind: Pipeline
metadata:
  name: complex-sum
spec:
  watermark:
    maxDelay: 30s
  limits:
    readTimeout: 50ms
    readBatchSize: 10
  vertices:
    - name: in
      limits:
        readBatchSize: 1
      scale:
        min: 1
      source:
        http: {}
    - name: atoi
      scale:
        min: 1
      udf:
        container:
          # Tell the input number is even or odd, see https://github.com/numaproj/numaflow-go/tree/main/examples/mapper/even_odd
          image: quay.io/numaio/numaflow-go/map-even-odd:stable
          imagePullPolicy: IfNotPresent
    - name: keyed-fixed-sum
      partitions: 1
      udf:
        container:
          # Compute the sum, see https://github.com/numaproj/numaflow-go/tree/main/examples/reducer/sum
          image: quay.io/numaio/numaflow-go/reduce-sum:stable
          imagePullPolicy: IfNotPresent
        groupBy:
          window:
            fixed:
              length: 10s
          keyed: true
          storage:
            emptyDir: {}
    - name: non-keyed-fixed-sum
      udf:
        container:
          # Compute the sum, see https://github.com/numaproj/numaflow-go/tree/main/examples/reducer/sum
          image: quay.io/numaio/numaflow-go/reduce-sum:stable
          imagePullPolicy: IfNotPresent
        groupBy:
          window:
            fixed:
              length: 60s
          keyed: false
          storage:
            emptyDir: {}
    - name: sink
      scale:
        min: 1
      sink:
        udsink:
          container:
            # A redis sink for e2e testing, see https://github.com/numaproj/numaflow-go/tree/main/examples/sinker/redis_sink
            image: quay.io/numaio/numaflow-go/redis-sink:stable
            imagePullPolicy: IfNotPresent
            env:
              - name: SINK_HASH_KEY
                # The key is set in the format of "pipeline_name-vertex_name"
                value: "complex-sum-sink"
  edges:
    - from: in
      to: atoi
    - from: atoi
      to: keyed-fixed-sum
    - from: keyed-fixed-sum
      to: non-keyed-fixed-sum
    - from: non-keyed-fixed-sum
      to: sink
