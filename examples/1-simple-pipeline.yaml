apiVersion: numaflow.numaproj.io/v1alpha1
kind: Pipeline
metadata:
  name: simple-pipeline
spec:
  vertices:
    - name: in
      scale:
        min: 1
      # A self data generating source
      source:
        generator:
          rpu: 50000
          msgSize: 1000
          duration: 1s

    - name: out
      scale:
        min: 1
      sink:
        blackhole: {}
  edges:
    - from: in
      to: out
