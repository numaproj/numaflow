/// gRPC clients and also protobuf objects for gRPC.
pub mod clients;

/// gRPC servers and also protobuf objects for gRPC.
pub mod servers;

/// protobuf objects for concrete types
pub mod objects;

/// common protobuf objects shared across modules
pub mod common;
